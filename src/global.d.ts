declare global {
    namespace globalThis {
        // eslint-disable-next-line no-var
        var CU_URL: string | undefined;
        // eslint-disable-next-line no-var
        var GATEWAY_URL: string | undefined;
        // eslint-disable-next-line no-var
        var MU_URL: string | undefined;
        // eslint-disable-next-line no-var
        var SU_URL: string | undefined;
        // eslint-disable-next-line no-var
        var GRAPHQL_URL: string | undefined;
    }
}

export {};
