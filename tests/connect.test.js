import { connect } from "../dist/index.js";
// { result, results, message, spawn, monitor, unmonitor, dryrun, assign }

const args = { GATEWAY_URL: "google.com", MU_URL: "google.com", CU_URL: "google.com", GRAPHQL_URL: "google.com" };

try {
  // const app = connect(args);
} catch (e) {

}
